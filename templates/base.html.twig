<!DOCTYPE html>
<html lang="es" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}CineProyecto - Sistema de Gestión Cinematográfica{% endblock %}</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
    .navbar{
        position: relative;
        z-index: 1050;
    }


        :root {
            --primary-color: #2c3e50;
            --secondary-color: #3498db;
            --accent-color: #e74c3c;
            --light-color: #ecf0f1;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding-top: 20px;
            padding-bottom: 50px;
        }
        
        .main-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            padding: 30px;
            margin-top: 20px;
        }
        
        .navbar-custom {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            margin-bottom: 30px;
           
        }
        
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: var(--primary-color) !important;
            
        }
        
        .film-card {
            background: white;
            border: none;
            border-radius: 15px;
            transition: all 0.3s ease;
            overflow: hidden;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
        }
        
        .film-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
        }
        
        .film-card .card-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            font-weight: 600;
            border: none;
        }
        
        .film-table {
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
        }
        
        .film-table thead {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
        }
        
        .film-table tbody tr {
            transition: all 0.2s;
        }
        
        .film-table tbody tr:hover {
            background-color: rgba(52, 152, 219, 0.1);
            transform: scale(1.01);
        }
        
        .btn-custom-primary {
            background: linear-gradient(135deg, var(--secondary-color), #2980b9);
            border: none;
            color: white;
            padding: 10px 25px;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .btn-custom-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 7px 20px rgba(52, 152, 219, 0.4);
        }
        
        .stat-card {
            background: white;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            text-align: center;
        }
        
        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--secondary-color);
            margin: 10px 0;
        }
        
        .film-badge {
            font-size: 0.75rem;
            padding: 5px 10px;
            border-radius: 20px;
            font-weight: 600;
        }
        
        .rating-badge {
            padding: 3px 8px;
            border-radius: 5px;
            font-weight: 600;
            font-size: 0.7rem;
        }
        
        .rating-PG { background-color: #2ecc71; color: white; }
        .rating-G { background-color: #3498db; color: white; }
        .rating-PG-13 { background-color: #f39c12; color: white; }
        .rating-R { background-color: #e74c3c; color: white; }
        .rating-NC-17 { background-color: #9b59b6; color: white; }
        
        .action-buttons .btn {
            border-radius: 8px;
            margin: 2px;
            padding: 5px 12px;
        }
        
        .search-box {
            background: white;
            border-radius: 50px;
            padding: 10px 25px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            border: 2px solid #eee;
        }
        
        .search-box:focus-within {
            border-color: var(--secondary-color);
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.2);
        }
        
        footer {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            margin-top: 40px;
            padding: 20px;
            text-align: center;
            color: var(--primary-color);
        }
        
        /* Alineación perfecta de iconos en dropdown */
        .dropdown-menu .dropdown-item {
            display: flex;
            align-items: center;
        }
        
        .dropdown-menu .dropdown-item i {
            width: 24px;
            min-width: 24px;
            text-align: center;
            margin-right: 8px !important;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container">
            <a class="navbar-brand" href="{{ path('film_index') }}">
                <i class="bi bi-camera-reels me-2"></i>
                <span style="background: linear-gradient(135deg, #3498db, #9b59b6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
                    CineProyecto
                </span>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link fw-bold" href="{{ path('app_home') }}">
                            <i class="bi bi-speedometer2 me-1"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active fw-bold" href="{{ path('film_index') }}">
                            <i class="bi bi-film me-1"></i> Películas
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('film_new') }}">
                            <i class="bi bi-plus-circle me-1"></i> Nueva Película
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-grid-3x3-gap me-1"></i> Más Opciones
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item d-flex align-items-center" href="{{ path('actor_index') }}">
                                <i class="bi bi-person-video2" style="width: 20px;"></i>
                                <span class="ms-2">Actores</span>
                            </a></li>
                            <li><a class="dropdown-item d-flex align-items-center" href="{{ path('customer_index') }}">
                                <i class="bi bi-people-fill" style="width: 20px;"></i>
                                <span class="ms-2">Clientes</span>
                            </a></li>
                            <li><a class="dropdown-item d-flex align-items-center" href="{{ path('rental_index') }}">
                                <i class="bi bi-cart-check" style="width: 20px;"></i>
                                <span class="ms-2">Alquileres</span>
                            </a></li>
                            <li><a class="dropdown-item d-flex align-items-center" href="{{ path('inventory_index') }}">
                                <i class="bi bi-box-seam" style="width: 20px;"></i>
                                <span class="ms-2">Inventario</span>
                            </a></li>
                            <li><a class="dropdown-item d-flex align-items-center" href="{{ path('app_payment_index') }}">
                                <i class="bi bi-credit-card" style="width: 20px;"></i>
                                <span class="ms-2">Pagos</span>
                            </a></li>
                            <li><a class="dropdown-item d-flex align-items-center" href="{{ path('app_staff_index') }}">
                                <i class="bi bi-person-badge" style="width: 20px;"></i>
                                <span class="ms-2">Staff</span>
                            </a></li>
                            <li><a class="dropdown-item d-flex align-items-center" href="{{ path('app_store_index') }}">
                                <i class="bi bi-shop" style="width: 20px;"></i>
                                <span class="ms-2">Tiendas</span>
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item d-flex align-items-center" href="#">
                                <i class="bi bi-gear" style="width: 20px;"></i>
                                <span class="ms-2">Configuración</span>
                            </a></li>
                        </ul>
                    </li>
                </ul>
                
                <div class="d-flex ms-3">
                    <div class="input-group search-box">
                        <span class="input-group-text border-0 bg-transparent">
                            <i class="bi bi-search text-muted"></i>
                        </span>
                        <input type="text" class="form-control border-0" placeholder="Buscar películas..." style="outline: none;">
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Contenido principal -->
    <div class="container">
        <div class="main-container">
            <!-- Mensajes flash -->
            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show d-flex align-items-center" role="alert">
                    <i class="bi bi-check-circle-fill me-2"></i>
                    <div>{{ message }}</div>
                    <button type="button" class="btn-close ms-auto" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
            
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger alert-dismissible fade show d-flex align-items-center" role="alert">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>
                    <div>{{ message }}</div>
                    <button type="button" class="btn-close ms-auto" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
            {% for type, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ type }} mt-3">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}


            {% block body %}{% endblock %}
        </div>
    </div>

       <!-- Footer -->
    <footer class="container mt-4">
    <div class="row align-items-center">
        <div class="col-md-6 text-md-start text-center">
            <h6 class="fw-bold mb-0">
                <i class="bi bi-camera-reels me-2"></i> CineProyecto
            </h6>
            <small class="text-muted">
                Sistema de Gestión Cinematográfica
            </small>
        </div>

        <div class="col-md-6 text-md-end text-center mt-2 mt-md-0">
            <small class="text-muted">
                © {{ "now"|date("Y") }} · Proyecto Académico · Symfony 7 · MySQL
            </small>
        </div>
    </div>
</footer>


    <!-- Bootstrap JS Bundle - VERSIÓN CORREGIDA -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" 
            integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" 
            crossorigin="anonymous"></script>
    
    
</body>
</html>